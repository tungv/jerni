{
  "name": "heq",
  "version": "2.0.0-beta.3",
  "description": "CLI to start an event.js server",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "build": "pkg -t node10 -o dist/heq .",
    "prebuild": "rimraf -rf dist && mkdir -p dist",
    "build:run": "dist/heq",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engine": {
    "node": "^10"
  },
  "author": "Tung Vu <me@tungv.com>",
  "license": "ISC",
  "dependencies": {
    "@heq/server-redis": "^1.0.2",
    "brighten": "^0.0.1",
    "heq-server": "^2.0.0",
    "ip": "^1.1.5",
    "micro": "^9.3.2",
    "port-authority": "^1.0.4",
    "sade": "^1.4.1"
  },
  "devDependencies": {
    "pkg": "^4.3.3"
  }
}
